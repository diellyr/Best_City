[project]
name = "best-city-sp"
version = "0.1.0"
description = "Agente auditável para análise comparativa de cidades paulistas"
requires-python = ">=3.11"

[project.scripts]
best-city = "best_city.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
