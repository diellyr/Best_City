[project]
name = "buscalar"
version = "0.1.0"
description = "Agente auditável para análise comparativa de cidades paulistas"
requires-python = ">=3.11"

[project.scripts]
buscalar = "buscalar.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
